/**
 * Important vendor file - should be indexed due to negation pattern !vendor/important.ts
 */

export function criticalFunction(): void {
  console.log('This is critical and should not be ignored');
}
